# Quiz Buzzer Trainer

日本語競技クイズ向け「一文字送り早押し練習アプリ」。

## Current status

Phase 1 baseline scaffold. Functional implementation is frozen until the GitHub Actions baseline passes all gates in one run:

1. `npm install`
2. `npm run typecheck`
3. `npm test`
4. `npm run build`

Current application code should be treated as **UNVERIFIED** until that baseline is green.
